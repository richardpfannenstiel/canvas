---
description: Check the simulator tooling and list the devices canvas can see
disable-model-invocation: false
---

Run the canvas `doctor` tool and report the result.

If anything is missing, explain in one line what capability it costs the user
and give the exact install command. Then list the booted devices, or say that
none are booted and offer to boot one.
